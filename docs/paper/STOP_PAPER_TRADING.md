# توقف معاملات آزمایشی

`stop_paper_trading.bat` را اجرا کنید. این فرمان State را `DISARMED` می‌کند و هیچ داده تاریخی را حذف نمی‌کند. اگر loop محلی باز است، `Ctrl+C` بزنید و سپس فرمان توقف را اجرا کنید.

